{
  "name": "Dart 2.12 + Java 17 Dev Container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "extensions": [
    "Dart-Code.dart-code",
    "vscjava.vscode-java-pack"
  ],
  "settings": {
    "files.eol": "\n",
    "dart.sdkPath": "/usr/local/dart-sdk"
  },
  "postCreateCommand": "dart --version",
  "forwardPorts": [8080],
  "remoteUser": "vscode",
  "description": "Container with Dart 2.12.0, OpenJDK 17, make and build-essential preinstalled for consistent and isolated dev environment."
}
