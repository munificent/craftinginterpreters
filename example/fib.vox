fun fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

var start = clock();
print(fib(35));
print(clock() - start);

// A straight Java implementation of fib() does this in 37.0 ms.
// The Java Vox interpreter took 524534. :(
