class Base {
  foo(a, b) {
    print "Base.foo(" + a + ", " + b + ")";
  }
}

class Derived < Base {
  foo() {
    print "Derived.foo()";
    super.foo("a", "b", "c", "d");
  }
}

Derived().foo();
// expect: Derived.foo()
// expect: Base.foo(a, b)
